# Solana RPC endpoint (defaults to devnet if not set)
SOLANA_RPC_URL=https://api.devnet.solana.com

# JWT signing secret (defaults to "swarmocracy-dev-secret" in dev)
JWT_SECRET=your-jwt-secret-here

# Realm authority secret key (base58-encoded)
# This is the secret key of the wallet that created the DAO.
# Needed for minting governance tokens when agents join on-chain realms.
# Your teammate should set this to their wallet's secret key after creating the DAO.
MOLTDAO_AUTHORITY_SECRET=your-authority-secret-key-here
